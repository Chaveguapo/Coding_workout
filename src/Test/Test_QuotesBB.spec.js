import { test, expect } from "vitest";
import QuotesBreakingBad from "../assets/Components/QuotesBreakingBad";

test("Fetch data from API and stored in a variable", () => {
  expect(QuotesBreakingBad());
});
